# Langley

MVP веб-тренажёра по запоминанию иностранных слов.

## Суть приложения

### Настройка

Пользователь выбирает:
- Свой родной язык.
- Изучаемый язык.
- Наборы слов из доступных.

### Тренировка

- На экране лента карточек со словами на изучаемом языке.
- Одновременно видно три текущих карточки.
- Пользователь может переворачивать каждую карточку.
- На оборотной стороне это же слово на родном языке.
- Пользователь может спайпать карточки вправо/влево.
- Свайп влево означает, что слово хорошо знакомо.
- Свайп вправо — что слово ещё не закреплено в памяти.
- Место освобождается — лента двигается (бесконечно).
- Хорошо знакомые слова выпадают реже, незакреплённые — чаще.
- Тренировка никогда не заканчивается, только явный выход.

## Ограничения

Приложение frontend-only. Будучи MVP, оно симулирует
запрос доступных наборов слов с сервера, но фактически они "зашиты"
в клиент.

Доступны языки: английский, испанский, русский.
Предусмотрено добавление других языков. Предустановлены наборы слов:
- 100 самых частых существительных;
- 100 самых частых глаголов.

Результатом сборки приложения являются статические файлы:
html, css, js, assets.

## Данные

Слова хранятся в массивах с объектами, ключами объектов являются языки:

```javascript
[
  // Существительные
  // ...
  {
    en: 'food',
    ru: 'еда',
    es: 'comida',
  },
  // ...
]
```

Это позволит отфильтровывать из такого массива те пары слов, которые нужны прямо
сейчас. И легко добавлять новые языки. А если в каком-то языке какого-то слова
почему-то нет, значение будет `null` и слово не войдёт в тренировку.

## Пожелания

При создании приложения используем лучшие практики веб-разработки,
а также лучшие архитектурные решения, подходящие для разработки с помощью ИИ.

Наполняем приложение богатой визуальной составляющей.
Возможно применение смелых дизайнерских решений: объём, 3D.
Красота + эстетика. Делаем переключение между светлой и тёмной темой.

Поддерживаем производительность приложения, оберегаем его от багов
и неточностей.
